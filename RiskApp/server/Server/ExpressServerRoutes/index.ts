/**
 * Any route handlers for the ExpressServer
 * 
 * The actual store object should be in a store
 * 
 * This index.ts should just re-export any exposed functions that are used by the store to encapsulate the logic - that way internal functions are not used outside of here
 */